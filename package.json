{
  "name": "figboost",
  "version": "0.5.3",
  "private": true,
  "description": "Development-only regression tools for FigBoost; no Node packages are shipped with the patcher.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:electron": "node tests/run-electron.cjs"
  },
  "devDependencies": {
    "electron": "^44.3.0",
    "jsdom": "^30.0.1"
  }
}
